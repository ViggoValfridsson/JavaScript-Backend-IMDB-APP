import classes from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={classes.footer}>
      <h5 className={classes.footerHeader}>Viggo-Valfridsson-JavaScript-Backend</h5>
    </footer>
  );
};

export default Footer;
